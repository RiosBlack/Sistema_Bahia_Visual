version: '3.1'

services:
  db:
    image: mysql
    restart: always
    environment:
      MYSQL_DATABASE: 'bvRH'
      MYSQL_USER: 'admin'
      MYSQL_PASSWORD: 'admin'
      MYSQL_ROOT_PASSWORD: 'admin123'
    ports:
      - '3306:3306'
    expose:
      - '3306'
    volumes:
      - type: bind
        source: "/home/douglasbv/Documentos/Douglas Pessoal/BancoDeDados/Docker"
        target: /var/lib/mysql

networks:
  docker-network:
    driver: bridge
